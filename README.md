# EjectCloud

Облачное файловое хранилище с поддержкой Telegram бота и HTTPS.

## Функции

- 🔐 **HTTPS** - самоподписанный SSL сертификат
- 📁 **Загрузка и управление файлами** с drag & drop
- 🔑 **Аутентификация через Telegram токены**
- 👨‍💼 **Административная панель** с мониторингом
- 🤖 **Интеграция с Telegram ботом**
- 📊 **Квоты пользователей** и статистика
- 🗑️ **Корзина** с возможностью восстановления
- 🔗 **Публичные ссылки** на файлы
- 📈 **Мониторинг системы** (CPU, память, диск)

## Быстрый старт

### 1. Генерация SSL сертификата
```bash
python generate_keystore.py
```

### 2. Настройка Telegram бота
Отредактируйте `src/main/resources/application.properties`:
```properties
telegram.bot.token=YOUR_BOT_TOKEN
telegram.admin.chatid=YOUR_CHAT_ID
```

### 3. Запуск
```bash
mvn spring-boot:run
```

### 4. Доступ
- **Пользовательский интерфейс**: https://localhost:8443
- **Админ-панель**: получите ссылку через `/admin` в боте

## API

- `POST /api/auth/login` - Генерация токена входа
- `POST /api/files/upload` - Загрузка файла
- `GET /api/files` - Список файлов
- `POST /api/files/move` - Перемещение файла
- `DELETE /api/files/{id}` - Удаление файла
- `POST /api/admin/users` - Создание пользователя (админ)

## Telegram команды

- `/link` - Получить ссылку на UI
- `/admin` - Получить ссылку на админ-панель (только для админа)

## Конфигурация

Основные настройки в `application.properties`:

```properties
# HTTPS настройки
server.port=8443
ejectcloud.base-url=https://localhost:8443

# Квоты и лимиты
ejectcloud.default.quota=1073741824  # 1GB
spring.servlet.multipart.max-file-size=500MB

# Telegram бот
telegram.bot.token=YOUR_TOKEN
telegram.admin.chatid=YOUR_CHAT_ID

# Безопасность
ejectcloud.token.inactive-minutes=30

# DDoS защита
server.tomcat.max-connections=50     # Максимум соединений
server.tomcat.max-threads=20         # Максимум потоков
server.tomcat.connection-timeout=20000  # Таймаут соединения
```

## Структура проекта

```
EjectCloud/
├── src/main/java/org/ejectfb/ejectcloud/
│   ├── controller/     # REST контроллеры
│   ├── service/        # Бизнес логика
│   ├── model/          # Модели данных
│   └── config/         # Конфигурация
├── src/main/resources/
│   ├── static/         # Frontend файлы
│   └── application.properties
├── Data/               # Пользовательские файлы
├── keystore.p12        # SSL сертификат (генерируется)
└── stats.xml           # Статистика системы
```

## Безопасность

- ✅ **HTTPS** обязательно для всех соединений
- ✅ **Токенная аутентификация** через Telegram
- ✅ **Автоматический выход** по неактивности
- ✅ **Валидация файлов** и имен
- ✅ **Изоляция пользователей** по папкам
- ✅ **DDoS защита** - rate limiting и connection limits
- ✅ **Security headers** - HSTS, X-Frame-Options, X-Content-Type-Options
- ✅ **Slow Loris защита** - ограничение одновременных соединений

## Мониторинг

Админ-панель предоставляет:
- 📊 **Дашборд** с общей статистикой
- 👥 **Управление пользователями** и квотами
- 📈 **Графики мониторинга** (CPU, память, диск)
- 🕐 **История за 30 дней** с различными периодами

## Разработка

### Требования
- Java 17+
- Maven 3.6+
- Python 3.6+ (для генерации сертификата)

### Сборка
```bash
mvn clean package
```

### Запуск в dev режиме
```bash
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

## Лицензия

MIT License